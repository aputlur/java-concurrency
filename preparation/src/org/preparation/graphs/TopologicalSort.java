package org.preparation.graphs;

public class TopologicalSort {

    void topologicalSort(/** list_of_vertices **/) {
        // declare visited

        //decare order = []

        //iterate over list of vertices
        //do dfs (v, visited,order)

        // order.reverse
        // return order;
    }

    void dfs(/** v, visited, order**/){

        // v is in visited

        // iterate neighbors


        // orders.append(v);
        // visited.add(v);

        // Modify the code to handle cycle
    }

}
